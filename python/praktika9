import random 
cards = [11,2,3,4,5,6,7,8,9,10,10,10,10]
player_hand=[]
dealer_hand=[]
sum_of_player_hand = 0
def hit():
    player_hand.append(random.choice(cards))
    print(player_hand)
    
def aces_problem_solving(hand):
    value=hand
    aces = player_hand.count('11')
    while sum_of_player_hand > 21 and aces > 0 :
        value =- 10 
        aces =-1 
    return value
    
blackjack_on = True
while blackjack_on != False:
    start = input('shte igraem li blackjack "da" ili "ne"\n').lower()
    if start == 'da':
        player_hand.append(random.choice(cards))
        player_hand.append(random.choice(cards))
        while sum_of_player_hand < 17 :
            sum_of_player_hand=sum(player_hand)
            player_hand.append(random.choice(cards))
            aces_problem_solving(hand=sum_of_player_hand)
        print(sum_of_player_hand)
        print(player_hand)